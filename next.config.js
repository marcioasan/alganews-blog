/* 11.18. Fallback no avatar - 11' */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["storage.googleapis.com"],
  },  
}

module.exports = nextConfig
